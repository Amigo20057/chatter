export enum ERRORS {
  USER_NOT_EXISTS = "User with this email does not exist",
  USER_ALREADY_EXISTS_BY_EMAIL = "User with this email already exists",
  USER_ALREADY_EXISTS_BY_TAG = "User with this tag already exists",
  INVALID_PASSWORD = "Invalid password",
}
